#!/usr/bin/env bash

cd $(dirname $0)/..

CODE="/code"

docker run -it \
           --rm \
           --mount source=pip-cache,target=/root/.cache/pip \
           --mount type=bind,source="$(pwd)"/,target=${CODE} \
           python:3 \
           ${CODE}/scripts/lint_python ${CODE}
