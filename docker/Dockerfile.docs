# syntax=docker/dockerfile:1
FROM ghcr.io/quakeservices/cdk:latest

WORKDIR /tmp

COPY docs/package.json /tmp
COPY docs/package-lock.json /tmp

RUN --mount=type=cache,target=/root/.npm \
  npm install --global prettier
